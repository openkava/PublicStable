operator swirleyMovementOp(io Scalar ms, io Integer id, io Xfo baseXfo, io Xfo xfo) {
  //  report("swirleyMovementOp(%f, %f, %f)\n", xfo.tr.x, baseXfo.tr.y, baseXfo.tr.z );
  xfo.tr = baseXfo.tr;
  xfo.tr.x += sin((ms / 100) + (id * 0.2)) * 3.0;
  xfo.tr.y += cos(ms / 230) * 3.0; ;
}