function foo( in String strings[] )
{
  report strings;
}

function bar( io String strings[] )
{
  foo( strings );
}

function entry()
{
  var String strings[];
  strings.push( "foo" );
  strings.push( "bar" );
  bar( strings );
  bar( strings );
}
