operator entry() {
  Float64 x = 0.1;
  report sqrt64(x);
  report abs64(x);
  report round64(x);
  report ceil64(x);
  report floor64(x);
  report pow64(x, x);
  report sin64(x);
  report cos64(x);
  report tan64(x);
  report log64(x);
  report asin64( sin64(x) );
  report acos64( cos64(x) );
  report atan64( tan64(x) );
  report atan264( tan64(x), 1.0 );
}
