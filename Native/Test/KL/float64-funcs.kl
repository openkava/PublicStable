operator entry() {
  Float64 x = 0.1;
  Float64 y = 1.0;
  report sqrt(x);
  report abs(x);
  report round(x);
  report ceil(x);
  report floor(x);
  report pow(x, x);
  report sin(x);
  report cos(x);
  report tan(x);
  report log10(x);
  report log(x);
  report exp(x);
  report asin( sin(x) );
  report acos( cos(x) );
  report atan( tan(x) );
  report atan2( tan(x), y );
}
