/*
 *  Copyright 2010-2012 Fabric Engine Inc. All rights reserved.
 */

operator entry()
{
  Byte a[];
  for ( Size i=0; i<256; ++i )
    a.push( Byte(i) );
  
  String b[];
  b.push( String(a) );
  report b;
}
